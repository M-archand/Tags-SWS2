<Project Sdk="Microsoft.NET.Sdk">
	<PropertyGroup>
		<TargetFramework>net10.0</TargetFramework>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>
		<Platforms>x64</Platforms>
		<PlatformTarget>x64</PlatformTarget>
		<TieredCompilation>true</TieredCompilation>
		<OutputPath>$(ProjectDir)..\BuildOutput\plugins\Tags\</OutputPath>
		<AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
		<CopyLocalLockFileAssemblies>false</CopyLocalLockFileAssemblies>
	</PropertyGroup>
	<ItemGroup>
		<PackageReference Include="SwiftlyS2.CS2" Version="*" ExcludeAssets="runtime" PrivateAssets="all" />
	</ItemGroup>

	<ItemGroup>
		<ProjectReference Include="..\TagsApi\TagsApi.csproj" />

		<Content Include="resources/translations/**/*">
			<CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
		</Content>
	</ItemGroup>

	<Target Name="PostBuild" AfterTargets="Build">
		<ItemGroup>
			<FilesToCopy Include="$(ProjectDir)..\Tags-example.toml" />
			<ConfigTargetFile Include="$(ProjectDir)..\BuildOutput\configs\plugins\Tags\config.toml" />
		</ItemGroup>
		<ItemGroup>
			<FilesToDelete Include="$(OutputPath)TagsApi.dll" />
			<FilesToDelete Include="$(OutputPath)TagsApi.pdb" />
			<FilesToDelete Include="$(ProjectDir)..\BuildOutput\configs\plugins\Tags\Tags-example.toml" />
		</ItemGroup>

		<Copy SourceFiles="@(FilesToCopy)" DestinationFiles="@(ConfigTargetFile)" />
		<Delete Files="@(FilesToDelete)" />
	</Target>
</Project>
